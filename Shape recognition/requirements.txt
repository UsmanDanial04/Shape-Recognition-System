# requirements.txt
# TensorFlow is NOT listed here — it does not yet support Python 3.14.
# The ShapeClassifier will automatically use a contour-based heuristic instead.
# If you are on Python 3.10 / 3.11 / 3.12, add: tensorflow==2.13.0

opencv-python==4.13.0.92
mediapipe==0.10.35
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
